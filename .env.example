# 複製這個檔案成 .env（本機開發用），並填入你的 Supabase 資訊
# 在 Vercel 部署時，把這些變數加到專案的 Environment Variables

VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key

# 管理後台（/admin）的登入帳號 email。就是你在 Supabase 建立的 admin 帳號 email。
# 這不是機密（登入畫面只問密碼），但放環境變數方便更換。
VITE_ADMIN_EMAIL=you@example.com
